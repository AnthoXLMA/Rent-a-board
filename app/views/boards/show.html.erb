    <h1 style="margin-left:100px;"><%= @board.name %><br>by <%= link_to @board.user.first_name, @board.user %></h1>
<div class="container-body" style="display:flex; margin-left:50px;">
  <div class="container-one-board" >
    <%= cl_image_tag @board.photo.key %>
    <div class="board-details">
      <a><%= @board.brand %><br></a>
      <a><%= @board.size %><br></a>
      <a><%= @board.supplier_id %><br></a>
      <a><%= @board.price %>€<br></a>
      <a><%= @board.address %><br></a>
    </div>
  </div>
  <div class="container" style="padding:20px;box-shadow: 2px 2px 10px 2px grey; margin-right:100px; border-radius:5px 5px 5px 5px;">
  <%= simple_form_for [@board, @booking], remote: true do |f| %>
    <div class="container_one">
      <%= f.collection_select :board_id, Board.all, :id, :name %>
      <%= f.input :start_on, as: :date %>
      <%= f.input :end_on, as: :date %>
      <%= f.submit "Book Now", class: "btn btn-primary" %>
    </div>
    <% end %>
  </div>
</div>
